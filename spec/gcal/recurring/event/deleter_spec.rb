RSpec.describe Gcal::Recurring::Event::Deleter do
  it "has a version number" do
    expect(Gcal::Recurring::Event::Deleter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
